<p id="notice"><%= notice %></p>

<h1>Maps</h1>

<table>
  <thead>
    <tr>
      <th>Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @maps.each do |map| %>
      <tr>
        <td><%= map.address %></td>
        <td><%= map.latitude %></td>
        <td><%= map.longitude %></td>
        <td><%= link_to 'Show', topic_map_path(topic_id: map.topic_id,id: map.id) %></td>
        <td><%= link_to 'Edit', edit_topic_map_path(topic_id: map.topic_id, id: map.id) %></td>
        <td><%= link_to 'Destroy', topic_map_path(topic_id: map.topic_id,id: map.id), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Map', new_topic_map_path(topic_id: @topic.id) %>
